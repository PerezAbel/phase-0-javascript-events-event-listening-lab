// Select the button
const button = document.getElementById('button');

// Attach an event listener to the button
button.addEventListener('click', function() {
  // Define the action(s) to take when the button is clicked
  console.log('Button clicked!');
});
